<%= form_for @plan_form, url: plan_path(@plan.id), method: :put, html: { class: "max-w-sm mx-auto mt-20" } do |f| %>
  <h2 class="text-2xl mb-5">Editar planificación</h2>

  <%= render "form", f: f %>

  <%= f.submit "Editar", class: submit_classes %>
<% end %>
